// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`.

export const environment = {
  production: false
};

// ng build --base-href https://rewarddash.cloudapps.cisco.com/qualifications/app/
// ng build --base-href https://rewardstg.cloudapps.cisco.com/qualifications/app/
// ng build --base-href https://rewarddev.cloudapps.cisco.com/qualifications/app/

// ng build --prod --base-href https://rewarddash.cloudapps.cisco.com/qualifications/app/
// http://rewarddash.cloudapps.cisco.com/

// ng build --base-href https://crld.cloudapps.cisco.com/qualifications/app/
