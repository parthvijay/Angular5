import { TableHeightDirective } from './table-height.directive';

describe('TableHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new TableHeightDirective();
    expect(directive).toBeTruthy();
  });
});
